# Persona Recall — Latest Updates

**Purpose:** Session-to-session execution ledger for the canonical plan in [`Persona_recall.md`](./Persona_recall.md).

## Continuity rules

1. Keep `Persona_recall.md` as the approved baseline plan; update it only when the architecture, contracts, or roadmap changes by agreement.
2. Update this file after every meaningful implementation slice, failed attempt, rate limit, reset, or validation run.
3. Never mark a task complete without recording validation evidence.
4. Preserve incomplete work and blockers; do not replace them with vague summaries.
5. End every update with a precise **Resume Here** task that can be executed without reconstructing prior context.

## Current status

| Field | Value |
| --- | --- |
| Baseline plan | `Persona_recall.md` |
| Current milestone | Milestone 7 — Rich package rendering |
| Current implementation slice | Cross-provider package completeness and Final TDD visibility |
| Overall status | MVP Feature Specs validated successfully with OpenAI Sol; Final TDD persistence remains blocked |
| Active task | Diagnose Aqua Fighter Final TDD generation/commit/UI state and enforce provider/model metadata |
| Primary fixture | OpenAI `Aqua_Fighter_Project_Package` (best current quality/current-flow example) |
| Comparison fixtures | Gemini `Urban_Rally_Racing_Project_Package` and Mistral `Bluetooth_Content_Share_Project_Package` |

## Decisions already agreed

1. Do not hardcode critique questions, architectures, or project outputs.
2. Enforce persona contracts, schemas, validation rules, handoff data, and memory boundaries instead.
3. Prioritize specialist-role connections before broad Concierge/memory restoration.
4. Make the Agile Product Owner / Given-When-Then feature chain the first implementation focus.
5. Preserve model freedom to identify different risks, ask different questions, and produce different project-specific solutions.
6. Restore original-style orchestration only after specialist outputs are structurally reliable.

## Evidence and baseline findings

- Original examples are in `Example_Dev_Dr_Outputs/`, including original Space Marines package formats.
- Current provider-specific runs are in `Output Files/Mistral/` and `Output Files/Gemini3.7Flash/`.
- Current Gemini 3.7 Space Marines is structurally stronger than current Mistral Space Miner, but differs from the original Space Marines because critique questions, answers, and architectural decisions differ.
- Those differences are expected and desirable when role contracts are preserved.
- Current specialist output is partly structured already through `MVPFeatureSpec`, but technical specifications, TDD traceability, brief contracts, and validators remain too loose.
- Current unified pipeline skips critique data and returns empty `critiqueQA`, so it must be corrected later during Milestone 5.
- Current `compactConversation` uses an 8,000-character persona limit; structured memory restoration is deferred to Milestone 6.
- The 2026-08-16 OpenAI Sol Aqua Fighter run is the strongest current qualitative output: its discovery answers, critique, fighting-game system vocabulary, design pillars, arena rules, pitch, and MVP contracts remain project-specific and internally consistent.
- Aqua persisted 6/6 valid MVP Feature Specs and six per-feature `tddContent` architect records with no generation diagnostic, proving the repaired MVP/technical-spec chain can pass against a cloud model.
- Aqua persisted no `technicalDesignDocument` sections and no briefs. This proves the missing Final TDD is not only a Rich Package rendering problem; the assembled Final TDD was not committed to the exported package.
- Aqua's package `meta` contains project name, timestamp, and project ID, but exported `generationMetadata` does not identify OpenAI/Sol. Provider, model, run, and stage outcomes must become global package metadata requirements rather than folder/manual knowledge.

## Task ledger

### Completed

| ID | Task | Evidence / validation |
| --- | --- | --- |
| P0.0 | Preserve approved canonical roadmap | `Persona_recall.md` created in this session. |
| P0.0a | Create continuity ledger | This file created in this session. |
| P0.1 | Create detailed specialist-role requirements registry | `Persona_role_registry.md` created with all 19 roles, input/output contracts, restrictions, validations, handoffs, failure behavior, user visibility, and current implementation status. |
| P1.1 | Audit BDD Feature Contract v1 compatibility | `BDD_feature_contract_audit.md` documents the data flow, current parser/viewer/export/TDD gaps, provider-output evidence, additive migration, and test plan. |
| P1.2 | Add stable BDD contract fields and legacy TDD traceability | Added optional BDD scenario ID/type, optional feature requirement categories, optional legacy `TDDFeature.featureId`, and preserved feature IDs in both existing MVP-feature-to-TDD bridges. |
| P1.3 | Add side-effect-free BDD validator and generic-filler detection | Added `services/bddFeatureValidator.ts` with baseline structural validation, duplicate feature/scenario ID detection, and generic-filler warnings. Added deterministic Node assertions for valid, generic, legacy, duplicate-ID, and missing-clause fixtures. No generation or pipeline enforcement was added. |
| P1.4 | Strengthen Agile Product Owner MVP feature-spec prompt | Updated the JSON-only generator prompt to require stable IDs, typed multi-scenario BDD coverage, one non-happy-path scenario, concrete Gherkin semantics, measurable outcomes where supported, and project-specific requirement categories. Parser acceptance and pipeline enforcement remain unchanged. |
| P1.5 | Add schema-specific MVP feature repair path | Moved JSON parsing and MVP-feature normalization to `services/bddFeatureValidator.ts`; retained valid scenario IDs/types and additive fields, warn when malformed scenarios are dropped, separated JSON parse errors from structural validation errors, and target repairs with original feature/project/MVP context. Generation now enforces the stronger contract after repair; pipeline rejection remains deferred. |

### Planned — Milestone 1: BDD foundation

| ID | Task | Status | Depends on |
| --- | --- | --- | --- |
| P0.1 | Create detailed specialist-role requirements registry | Complete | P0.0 |
| P1.1 | Audit existing `MVPFeatureSpec`, `BDDScenario`, `TDDFeature`, viewer, exporter, and pipeline compatibility | Complete | P0.1 |
| P1.2 | Add stable feature ID and BDD scenario category contract changes | Complete | P1.1 |
| P1.3 | Add focused BDD validation utility and generic-filler detection | Complete | P1.2 |
| P1.4 | Rewrite Agile Product Owner prompt for concrete multi-scenario Given/When/Then output | Complete | P1.2, P1.3 |
| P1.5 | Add schema-specific MVP feature repair path | Complete | P1.3, P1.4 |
| P1.6 | Make pipeline reject invalid feature specs and retain validation outcome | Complete | P1.3, P1.5 |
| P1.7 | Update BDD viewer and Markdown/TXT export presentation | Complete | P1.2, P1.6 |
| P1.8 | Add Space Marines and Space Miner structural fixture checks | Complete | P1.6, P1.7 |
| P1.9 | Run lint, build, and focused regression validation | Complete | P1.8 |

### Future milestones

| Milestone | Scope | Status |
| --- | --- | --- |
| M2 | Technical specification foundation: data models, APIs, state transitions, architect pass, validators, TDD assembler | Complete — accepted with documented schema/output limitations |
| M3 | Production handoffs: structured briefs, role separation, dependencies, asset metadata, visual traceability | Complete — accepted with documented dependency-resolution limitation |
| M4 | Scope and pitch: four lens contracts, validation, claim grounding, slide validation | Complete — accepted with documented source-grounding limitation |
| M5 | Orchestration: critique data, structured stage handoffs, generation metadata | In progress — critique gate and envelopes integrated; final persistence/acceptance remains |
| M4 | Scope and pitch: four lens contracts, validation, claim grounding, slide validation | Not started |
| M5 | Orchestration: critique data, structured stage handoffs, generation metadata | Not started |
| M6 | Memory and persona restoration: structured memory, transcript, Concierge modes, user proxy, risk critique, synthesis | Not started |
| M7 | Rich package rendering: chat, critique, rendered Markdown, tables, images, navigation | In progress — rich HTML export foundation complete; app/share view integration remains |

## Files expected to change in BDD Feature Contract v1

- `types.ts`
- `services/lmStudioService.ts`
- `services/pipelineOrchestrator.ts`
- `services/packageExporter.ts`
- `components/MVPFeatureSpecViewer.tsx`
- a new focused validation/test utility, using existing project conventions

## Validation record

| Date | Action | Result |
| --- | --- | --- |
| 2026-08-16 | Created `Persona_recall.md` and this continuity ledger | Verified opening, specialist-pipeline, milestone, task-ledger, and Resume Here sections. |
| 2026-08-16 | `npm run lint` | Passed (`tsc --noEmit`). Documentation-only changes introduced no TypeScript errors. |
| 2026-08-16 | Created `Persona_role_registry.md` | Verified current code contracts against the registry. Key findings: BDD structure exists but its scenario contract is incomplete; MVP/TDD/brief structures are loose; unified pipeline omits critique data; rich memory restoration remains intentionally deferred. |
| 2026-08-16 | Completed P1.1 compatibility audit | `BDD_feature_contract_audit.md` defines additive-only type migration, parser/validation boundaries, prompt/repair sequence, viewer/export compatibility, legacy TDD bridge preservation, and Node assertion fixture approach. |
| 2026-08-16 | Completed P1.2 additive BDD contract migration | `npm run lint` passed. `npm run build` passed; Vite emitted only its existing bundle-size warning. |
| 2026-08-16 | Completed P1.3 BDD validator and generic-filler detection | `npm run test:bdd-validator`, `npm run lint`, and `npm run build` passed. Vite emitted only its existing bundle-size warning. |
| 2026-08-16 | Completed P1.4 stronger MVP feature-spec prompt contract | Prompt now requests stable IDs, explicit scenario types, at least two scenarios including a non-happy path, concrete Gherkin semantics, measurable outcomes where available, and project-specific requirement categories. Parser, repair, and pipeline behavior intentionally remain unchanged until P1.5/P1.6. |
| 2026-08-16 | Completed P1.5 schema-specific MVP repair and normalization | `npm run test:bdd-validator`, `npm run lint`, and `npm run build` passed. Vite emitted only its existing bundle-size warning. |
| 2026-08-16 | Completed P1.6 generated feature-spec validation gate | Both sequential and unified paths retain generation validation outcomes, reject invalid/unrepaired or duplicate generated specs before the TDD bridge, and leave legacy saved-project loading unchanged. `npm run lint`, `npm run test:bdd-validator`, and `npm run build` passed. |
| 2026-08-16 | Completed P1.7 BDD viewer and export presentation | Viewer conditionally renders feature/scenario IDs, scenario types, acceptance criteria, failure states, telemetry, security considerations, and performance targets. Markdown/TXT exports render the same populated fields; legacy specs do not gain empty additive headings. `npm run lint`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed P1.8 Space Marines and Space Miner structural fixtures | The deterministic Node fixture suite now verifies a valid Space Marines-like feature with named entities, numeric limits, typed happy/boundary Gherkin scenarios, and a Space Miner-like structurally valid but generic feature whose filler is surfaced as warnings. Existing legacy, duplicate-ID, malformed-Gherkin, normalization, and export checks remain covered. `npm run lint`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed P1.9 Milestone 1 regression validation | `npm run lint`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Production build completed without errors. A live LM Studio endpoint was available and `npm run test:e2e` was attempted; it reached the app/proxy and completed 24+ model requests but did not reach final assertions after more than six minutes, so the run was stopped as inconclusive. The E2E harness also reported port 3000 already in use and started its child Vite server on 3001 while its configured application URL remained 3000. |
| 2026-08-16 | Started Milestone 2 technical contract foundation | Added additive data-model, API-contract, state-transition, technical-specification, validation-outcome, and TDD bridge types. Added `services/technicalSpecContract.ts` for BDD-preserving assembly, validation, and legacy-compatible TDD bridging. Added deterministic `npm run test:technical-spec` coverage. `npm run lint`, `npm run test:bdd-validator`, `npm run test:technical-spec`, `npm run build`, and `git diff --check` passed. No prompts, orchestration control flow, viewers, exports, or memory behavior were changed. |
| 2026-08-16 | Completed M2.2 architect generation and repair seam | Added `generateTechnicalSpecification` with JSON-only Software Architect prompting, targeted repair diagnostics, normalization, architect-field validation, and source-authoritative preservation of `featureId`, feature metadata, and every BDD scenario. Existing legacy TDD/document generation remains unchanged. `npm run lint`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M2.3 validated technical/TDD assembly integration | Both `App.tsx` and `services/pipelineOrchestrator.ts` now run the architect pass for each validated MVP feature, reject invalid or missing technical artifacts, and assemble structured `TDDFeature.technicalSpecification` values while preserving legacy user-story/technical-spec fields. Focused tests cover valid assembly, missing output, feature-ID mismatch, and collection rejection. `npm run lint`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M2.4 structured final TDD input migration | Added `prepareTechnicalDesignInputs` as the stable final-document projection. `generateTechnicalDesignDocument` now prefers structured technical specifications, explicitly preserves feature and scenario IDs, and retains legacy fields as a fallback for older saved projects in both generation and repair prompts. Added projection assertions. After correcting a template-literal boundary, `npm run lint`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M2.5 technical validator and regression hardening | Added collection-level technical validation for duplicate feature IDs, duplicate scenario IDs within/across features, and architect-field requirements. Validated TDD assembly now applies collection checks, while legacy projection compatibility remains covered. `npm run lint`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Accepted Milestone 2 | Reviewed all five roadmap requirements: typed technical/data-model/API/state-transition contracts, architect prompt/repair, technical validation, feature/scenario traceability, and validated TDD assembly. Evidence: `types.ts`, `services/technicalSpecContract.ts`, `services/lmStudioService.ts`, `App.tsx`, `services/pipelineOrchestrator.ts`, and `scripts/test-technical-spec-contract.mjs`; `npm run lint`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Limitations recorded below. |
| 2026-08-16 | Completed M3.1 production handoff contract audit/foundation | Audited loose `FreelanceBrief`, modular breakdown, `AssetList`, and visual-prompt map contracts. Added additive `ProductionBrief`, `AssetMetadata`, and `VisualPromptContract` types plus normalization and validation in `services/productionHandoffContract.ts`. Added duplicate-ID, required-field, role-overlap warning, and asset-linked visual-prompt fixtures in `npm run test:production-handoff`. No prompts, generation flow, viewers, or exporters changed. `npm run lint`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M3.2 structured production brief generation and repair | Added `generateProductionBriefs` with role/category/deliverable/dependency/out-of-scope schema, normalization, validation, targeted repair, and GDD/handoff context. Existing `generateModularBreakdown` now maps validated structured briefs back to legacy title/content Markdown for current viewers and exports. Added parser/repair-boundary fixtures. `npm run lint`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M3.3 structured asset metadata and visual-prompt generation | Added asset metadata and visual prompt parsers, targeted repair, duplicate/unknown-reference validation, stable-ID source traceability, and legacy projections. `generateAssetMetadata` and `generateVisualPromptContracts` now provide structured outputs; existing `generateAssetList` and `generateAllVisualPrompts` preserve their legacy return types through validated projections. Added parser and projection fixtures. `npm run lint`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M3.4 structured production handoff pipeline/state integration | Added optional structured brief, asset metadata, and visual-prompt fields to `ProjectSession` and `ProjectPackage`. Unified pipeline now returns structured records plus legacy projections; App handlers populate structured records, project loading/autosave persists them, package assembly exports them, and refactor resets clear them with their legacy artifacts. Current viewers remain unchanged. `npm run lint`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Accepted Milestone 3 | Reviewed all six roadmap requirements: structured briefs, role separation, dependency validation, deliverables/acceptance criteria, asset metadata, and visual-prompt traceability. Added final validation for related-brief references and complete requested-asset prompt coverage. Evidence: `types.ts`, `services/productionHandoffContract.ts`, `services/lmStudioService.ts`, `services/pipelineOrchestrator.ts`, `App.tsx`, and `scripts/test-production-handoff-contract.mjs`; `npm run lint`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Limitation: asset dependency strings are validated for presence but are not yet resolved against one unified brief/asset registry. |
| 2026-08-16 | Completed M4.1 scope/pitch contract audit/foundation | Added lens-aware critique normalization and validation for `studio`, `indie`, `freelance`, and `gamejam`; additive grounded `PitchClaim` records; exact pitch slide title/count validation; source-reference checks; and deterministic `npm run test:scope-pitch` fixtures. Existing generation flow remains unchanged pending M4.2. `npm run lint`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M4.2 scope/slide validation integration | `generateScopeReview` now normalizes lens-tagged critiques, validates them, and repairs invalid responses; the unified pipeline rejects invalid scope output. Pitch generation normalizes optional structured claims and enforces exact configured slide count/title coverage after generation while preserving legacy slide fields. Added normalized-slide assertions. `npm run lint`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Grounded claims remain optional until M4.3. |
| 2026-08-16 | Completed M4.3 grounded pitch claims and diagnostic persistence | Unified pitch generation now supplies source IDs, requests grounded claims, requires claims when source IDs are provided, validates claims/source references, and rejects invalid decks. Added optional `scopeReviewValidation` and `pitchDeckValidation` records to `ProjectSession`/`ProjectPackage`; App loading, autosave, unified pipeline application, and package assembly persist them. Legacy standalone pitch callers remain compatible when no source IDs are supplied. `npm run lint`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Accepted Milestone 4 | Reviewed all four roadmap requirements: explicit `studio`/`indie`/`freelance`/`gamejam` scope prompts, scope validation, grounded pitch claims, and exact slide validation. Evidence: `types.ts`, `services/scopePitchContract.ts`, `services/lmStudioService.ts`, `services/pipelineOrchestrator.ts`, `App.tsx`, and `scripts/test-scope-pitch-contract.mjs`; `npm run lint`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Limitation: claim grounding validates source IDs and the model’s grounded flag, but does not yet semantically prove each claim against source text. |
| 2026-08-16 | Completed M5.1 orchestration contract audit/foundation | Added `CritiqueRecord`, `StageOutputEnvelope`, and `GenerationMetadata` contracts plus validators for completed critique answers, ordered stages, output references, timestamps, and run IDs in `services/orchestrationContract.ts`. Added deterministic `npm run test:orchestration` coverage. Existing pipeline control flow remains unchanged pending M5.2. `npm run lint`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M5.2 critique gate and structured stage-envelope integration | `runUnifiedPipeline` now requires a completed validated `CritiqueRecord`, raises `UnifiedPipelineCritiqueGateError` otherwise, preserves critique data in legacy `critiqueQA`, records ordered completed stages with output references, and validates `GenerationMetadata`. App derives/persists critique records, applies returned metadata, and reports gate failures without entering generation. `npm run lint`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Accepted Milestone 5 | Reviewed all five roadmap requirements: non-empty critique handoff, completed-critique gate, structured stage outputs, chat/critique preservation, and generation metadata. Unified packages preserve caller chat history and critique QA/records; sessions and exports persist critique records and metadata. Evidence: `types.ts`, `services/orchestrationContract.ts`, `services/pipelineOrchestrator.ts`, `App.tsx`, and `scripts/test-orchestration-contract.mjs`; `npm run lint`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Limitations: stage envelopes are currently coarse artifact boundaries, and structured orchestration records are persisted but not yet rendered in dedicated UI sections. |
| 2026-08-16 | Completed M6.1 memory/persona contract audit/foundation | Added structured memory entries, full transcript records, Concierge modes, User Proxy, risk critique, and synthesis types plus validators in `services/memoryPersonaContract.ts`. Added deterministic `npm run test:memory-persona` coverage. Existing persona control flow and 8,000-character compaction remain unchanged pending M6.2. `npm run lint`, `npm run test:memory-persona`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M6.2 transcript/memory persistence integration | Added optional `transcriptRecord`, `memoryEntries`, and `conciergeMode` fields to sessions/packages. New projects initialize full transcripts; legacy sessions backfill transcripts from chat history. Chat and critique updates refresh transcript records; session autosave, package assembly, unified pipeline application, and project loading preserve the new fields. Added role-relevant memory context helper and tests. `npm run lint`, `npm run test:memory-persona`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. Broad persona prompts still use bounded conversation pending M6.3. |
| 2026-08-16 | Completed M6.3 structured memory extraction and Concierge mode integration | Added validated `extractStructuredMemory` with JSON repair fallback, source references, duplicate/status normalization, and durable memory categories. Conversation prompts now receive selected `conciergeMode` guidance plus role-relevant structured memory; extracted entries are persisted after each chat turn while full transcript remains intact. Added mode guidance and normalization fixtures. `npm run lint`, `npm run test:memory-persona`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M6.4 User Proxy/risk/synthesis integration | Added validated `generateUserProxy`, adversarial `generateRiskCritique`, and `generateSynthesis` prompts with structured repair boundaries. User Proxy and risk records are generated with technical critique context; synthesis consumes GDD, memory, critique, and risk outputs with traceable references. Records are persisted in session state and package assembly. Added valid/invalid validator fixtures. `npm run lint`, `npm run test:memory-persona`, `npm run test:orchestration`, `npm run test:scope-pitch`, `npm run test:production-handoff`, `npm run test:technical-spec`, `npm run test:bdd-validator`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Accepted Milestone 6 | Reviewed all seven requirements: structured memory, full transcript, Concierge modes, User Proxy, adversarial risk critique, separate critique/answers, and synthesis. Evidence: `types.ts`, `services/memoryPersonaContract.ts`, `services/lmStudioService.ts`, `App.tsx`, and `scripts/test-memory-persona-contract.mjs`; all focused suites, lint, build, and `git diff --check` passed. Limitations: structured records are persisted and exported but not yet rendered in dedicated UI panels; full persona restoration remains contract-driven rather than a complete mode-routing system. |
| 2026-08-16 | Completed M7.1 rich HTML package export foundation | Replaced escaped `<pre>` HTML export with `marked` Markdown rendering, generated navigation/TOC anchors, responsive styling, code blocks, tables, chat/critique sections, memory/persona records, collapsible-ready semantic headings, and generated image figures. Added `npm run test:package-rendering` structural assertions for navigation, tables, code, images, chat, and persona records. Updated the BDD fixture harness to resolve the confirmed `marked` dependency in temporary compilation output. `npm run lint`, `npm run test:package-rendering`, all prior focused suites, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M7.2 rich package view and share integration | Added the rich package preview as the default results presentation in `App.tsx`, retained legacy section viewers behind an explicit toggle, mounted `ShareButton` with the same memoized `ProjectPackage` snapshot, and made the share landing page pass validated rich payloads through without dropping structured persona, memory, MVP validation, production, or critique records. Reused `exportHTML` through `RichPackagePreview` for on-screen navigation, Markdown rendering, tables, code, images, chat, critique, and collapsible sections. `npm run lint`, `npm run test:package-rendering`, `npm run test:share-package`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M7.3 representative fixture coverage and optional-record rendering | Added `npm run test:rich-package-coverage` using the current Gemini Space Marines and Mistral Space Miner JSON packages, verifying GDD, pitch, MVP BDD, TDD, briefs, assets, scope review, images, navigation, and rich-share compatibility. Added enriched-package assertions for transcript, generation metadata, memory/persona records, MVP validation, structured production handoffs, asset metadata, and visual prompt contracts. Extended `exportMarkdown`/`exportHTML` coverage through the shared renderer without changing generation contracts. `npm run test:rich-package-coverage`, `npm run test:package-rendering`, `npm run test:share-package`, `npm run lint`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Completed M7.4 browser-level rich presentation smoke validation | Mounted `ShareLandingPage` for `#share_id=` routes so generated share links reach the rich presentation. Added native collapsible `<details>` panels to shared HTML rendering while preserving TOC anchors and image placeholder handling. Added deterministic Playwright smoke coverage that hydrates a representative Space Marines project through compressed localStorage, verifies iframe/srcDoc navigation, collapsible sections, valid image rendering, legacy viewer access, and a mocked rich share route. `npm run test:rich-presentation-smoke`, `npm run test:rich-package-coverage`, `npm run test:package-rendering`, `npm run test:share-package`, `npm run lint`, `npm run build`, and `git diff --check` passed. Note: Playwright reports Node's existing `shell: true` child-process deprecation warning only. |
| 2026-08-16 | Accepted Milestone 7 — Rich package rendering | Reviewed all six requirements: chat/critique display is covered by `exportMarkdown`/`exportHTML`; rich Markdown HTML uses `marked`; tables and code blocks are covered structurally; generated images and placeholder handling are covered; native collapsible sections are verified in browser; complete navigation is covered by TOC anchors and browser iframe smoke. Main app, share route, legacy viewers, and HTML/Markdown/TXT/JSON export paths remain available. Evidence: M7.1–M7.4 entries and `npm run test:rich-presentation-smoke`, `npm run test:rich-package-coverage`, `npm run test:package-rendering`, `npm run test:share-package`, `npm run lint`, `npm run build`, and `git diff --check`. Limitations: live backend share persistence and real LM Studio generation remain outside deterministic presentation smoke coverage; `generateImage` remains a documented placeholder. |
| 2026-08-16 | Extended real LM Studio E2E with rich-output assertions | Updated `scripts/test-manual-sequential-e2e.mjs` to allocate a free Vite port, use the actual app origin for CORS preflight, fail fast on browser/model validation errors, verify generated rich iframe navigation/collapsibles/images, preserve legacy viewer access, and structurally inspect HTML/Markdown/TXT/JSON downloads. Real run reached the pitch stage and correctly rejected an actual model response containing 21 ungrounded claims and one missing required slide (`The Solution: Your Project`), so no complete real package was produced. This is a genuine provider-output contract failure, not a presentation failure. Deterministic rendering/share suites remain green. |
| 2026-08-16 | Added pitch validation repair/retry hardening | Extended `generateFullPitchDeck` with one bounded post-generation repair pass for structurally parsed slides whose claims are ungrounded or whose configured title/count coverage is invalid. The repair prompt requires exact configured titles, grounded claims, source references, and no invented facts; strict validation still rejects the response if repair fails. Added deterministic rejected-real-shape assertions to `scripts/test-scope-pitch-contract.mjs`. `npm run test:scope-pitch`, `npm run lint`, `npm run test:rich-presentation-smoke`, `npm run test:rich-package-coverage`, `npm run test:package-rendering`, `npm run test:share-package`, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Added second bounded pitch repair retry after real-provider rerun | The next real run reduced the original failure to one residual claim on Slide 6 (`grounded=false`, empty source references). Added a second validation repair attempt with an explicit rule to omit unsupported claims rather than fabricate them. Focused scope/pitch, rich rendering/share, lint, build, and diff checks passed. The strict validator remains unchanged. |
| 2026-08-16 | Added transient LM Studio transport retry after real-provider rerun | The next real run passed pitch repair and advanced into MVP feature-spec generation, then hit `net::ERR_NETWORK_IO_SUSPENDED` / `TypeError: Failed to fetch` from the local proxy during a long request sequence. Added two bounded exponential-delay retries for transient LM Studio network errors at `services/aiProvider.ts`; non-local providers, validation errors, HTTP/model errors, and timeouts are not weakened. `npm run lint`, `npm run test:scope-pitch`, `npm run test:orchestration`, all rich rendering/share suites, `npm run build`, and `git diff --check` passed. No complete real package was produced because the proxy interruption occurred before final generation. |
| 2026-08-16 | Caffeinated real-provider E2E rerun completed without sleep interruption | Ran `npm run test:e2e` under `caffeinate -dimsu`; Vite, proxy, and LM Studio remained active for the full run. The run reached pitch generation but still failed strict validation after both repair attempts: Slide 7 claim 4 was ungrounded; Slide 9 claim 2 was ungrounded and had no source references. No complete package was produced. This confirms the remaining issue is model repair quality, not screen sleep, port selection, transport retry, or rich presentation. |
| 2026-08-16 | Replaced per-slide pitch repair with whole-deck schema-constrained repair | `generateFullPitchDeck` now sends the complete attempted deck, configured slide schema, source IDs, and full validation errors to a whole-deck repair request, with one bounded retry. The repaired response must contain the exact slide count/order/titles; unsupported claims must be omitted, and any claims present must be grounded with source references. Added deterministic valid repaired-deck coverage. `npm run test:scope-pitch`, `npm run lint`, `npm run test:orchestration`, all rich rendering/share suites, `npm run build`, and `git diff --check` passed. |
| 2026-08-16 | Caffeinated whole-deck real E2E passed pitch and reached MVP feature specs | `caffeinate -dimsu` prevented sleep. Whole-deck pitch repair succeeded; the run advanced through many MVP feature-spec requests without browser/model/transport errors, then the test harness timed out at 240 seconds waiting for the sequential MVP Feature Specs button to turn green. Increased only that step's wait to 600 seconds. No complete package was produced. `node --check`, all focused pitch/orchestration/rendering/share suites, lint, build, and `git diff --check` passed. |
| 2026-08-16 | Caffeinated real E2E reached no product error but exceeded initial compile wait | The next caffeinated run produced no browser/model/transport failures, but the initial GDD/critique compile did not reach the rich results view within the existing 360-second wait. Increased only the initial rich-results wait to 600 seconds, matching the long-running local-model profile. |
| 2026-08-16 | Added second MVP feature-spec repair attempt after caffeinated real E2E | The latest run reached MVP feature-spec generation and exposed a real invalid response for `Procedural celestial bodies for unlimited exploration possibilities`: no scenarios. Added a second bounded repair request with the complete validation targets and explicit two-scenario happy/non-happy requirements. Added deterministic zero-scenario validator coverage. `npm run test:bdd-validator`, `npm run test:production-handoff`, `npm run test:scope-pitch`, `npm run test:orchestration`, all rich rendering/share suites, lint, build, and `git diff --check` passed. |
| 2026-08-16 | Added third MVP feature-spec repair retry after caffeinated real E2E | The latest run reached MVP specs but several features still failed after two repairs: three had no scenarios and two lacked technical notes. Extended the bounded per-feature repair loop to three attempts; strict validation remains unchanged. |
| 2026-08-16 | Caffeinated real E2E exposed missing-slide whole-deck repair failure | The latest run reached pitch generation, but the whole-deck repair returned 9 instead of 10 slides and omitted configured `Contact Information` after both attempts. Added deterministic missing-configured-slide coverage. The next repair patch must reconstruct missing configured slides or reject with clearer repair diagnostics without weakening exact count/title validation. |
| 2026-08-16 | Added targeted missing configured pitch-slide completion | Whole-deck repair now detects absent configured titles, requests each missing slide with its exact title/objective/source constraints, rebuilds the deck in configured order, and then applies the existing strict count/title/grounding validator. No unsupported contact details or claims are invented by the completion prompt. `npm run test:scope-pitch`, `npm run test:bdd-validator`, `npm run test:production-handoff`, `npm run test:orchestration`, rich rendering/share suites, lint, build, and `git diff --check` passed. |
| 2026-08-16 | Caffeinated real E2E passed missing-slide pitch completion and reached Asset List | The latest run produced no browser/model/transport errors, passed pitch including targeted missing-slide completion, then exceeded the default 180-second Asset List wait during long local-model generation. Increased only the Asset List wait to 600 seconds. |
| 2026-08-16 | Set global real-E2E inference timeout to 600 seconds | Repeated caffeinated runs showed that local sequential inference and schema repair routinely exceed 180–360 seconds at different stages. Changed `scripts/test-manual-sequential-e2e.mjs` so the shared `inferenceTimeout` defaults to 600000 ms; compile, pitch, assets, MVP specs, TDD, briefs, helper waits, preview checks, and download checks now inherit the same policy. `E2E_INFERENCE_TIMEOUT_MS` remains available for deliberate overrides. |
| 2026-08-16 | Global-timeout E2E exposed targeted missing-slide retry gap | The latest run reached pitch repair but still omitted configured `The Team` after whole-deck repair and first targeted completion. Added a second targeted retry for missing slides when the first completion response is empty or unparsable; exact count/title validation remains strict. |
| 2026-08-16 | Global 600-second E2E reached MVP but still timed out | The latest caffeinated run produced no browser/model/transport errors and reached sequential MVP feature-spec generation, but the entire MVP stage exceeded the global 600-second limit. This is now a performance/test-design blocker, not a repeated per-stage timeout configuration issue. No complete package was produced. The next slice should reduce or separately scope the real E2E feature count, or add stage progress/long-run handling, rather than blindly increasing the timeout. |
| 2026-08-16 | Added fourth MVP repair attempt after global-timeout E2E | The global 600-second run reached MVP specs and still found one feature with zero scenarios after three repairs. Added a fourth bounded repair attempt with an explicit minimal-contract instruction requiring exactly two scenarios and non-empty technical notes when the prior response is empty. |
| 2026-08-16 | Added visual-prompt contract retry after caffeinated real E2E | The latest run passed whole-deck pitch and advanced to visual prompt generation, where the model returned an invalid asset-linked prompt set and its first repair also failed. Added a second bounded whole-set repair using complete asset IDs and validation errors; strict duplicate/unknown/missing asset validation remains intact. `npm run test:production-handoff`, `npm run test:scope-pitch`, `npm run test:orchestration`, rich rendering/share suites, lint, build, and `git diff --check` passed. |
| 2026-08-16 | Caffeinated real E2E reached Pitch Deck but exceeded its wait | The latest run produced no browser/model/transport error, but the Pitch Deck step exceeded the existing 360-second wait while processing the large whole-deck repair path. Increased only the Pitch Deck wait to 600 seconds, matching compile and MVP feature-spec waits. |
| 2026-08-16 | Completed full canonical real-provider E2E after long-run redesign | `npm run test:e2e` completed successfully under `caffeinate -dimsu` for Bluetooth Content Share. The run passed progress-aware compile waiting beyond the former 600-second boundary, full sequential MVP feature/technical generation, Final TDD, Freelance Briefs, rich preview, legacy viewers, HTML, Markdown, TXT, and JSON export assertions. No browser, transport, model-validation, or sleep-interruption failures occurred. This accepts the E2E performance/test-design redesign as the new baseline. |
| 2026-08-16 | Manual Gemini 3.7 review added UI/data-capture follow-up track | A quick manual Gemini 3.7 run exposed product-flow gaps not reproduced by the complete LM Studio E2E: the UI needs several manual presentation adjustments, and a manual run appeared to capture/export only part of the generated package, with MVP Feature Specs and TDD not visibly available. Queue UI state visibility, stage persistence, package completeness, and manual Gemini export verification after persona-recall fixes; do not reopen the resolved sequential-inference timeout work. |
| 2026-08-16 | Resumed persona-recall fixes with memory merge and Concierge mode routing | New sessions now start in `project-name` mode. Structured memory extraction merges incoming entries with existing memory by stable ID and unions source references instead of replacing prior memory. Added deterministic Concierge mode derivation for project-name, information-gatherer, creative-brainstormer, and completion-gate states. `npm run test:memory-persona`, orchestration, BDD, technical-spec, lint, build, rich presentation, and `git diff --check` passed. Remaining persona work: defer User Proxy/risk generation until completed critique answers and render persisted persona records in dedicated UI panels. |
| 2026-08-16 | M6 persona handoff fix: User Proxy and risk critique now receive completed answers | Removed early User Proxy/risk generation from critique-question creation. Completed critique submission now builds a traceable specialist context containing conversation, non-rejected memory, and the completed questions/answers record before generating User Proxy and Senior Technical Analyst risks. Synthesis uses the completed critique and same-run risk record. Added deterministic specialist-context coverage. `npm run test:memory-persona`, orchestration, lint, build, rich presentation, and `git diff --check` passed. |
| 2026-08-16 | M6 persona records UI panels and browser coverage | Added a dedicated read-only `PersonaRecordsViewer` in the results page for Concierge mode, structured memory, User Proxy, Senior Technical Analyst risks, synthesis, and full transcript status. Enriched the Playwright rich-presentation fixture and assertions to verify all persona records render after project hydration. `npm run test:rich-presentation-smoke`, `npm run test:memory-persona`, orchestration, lint, build, and `git diff --check` passed. |
| 2026-08-16 | macOS Keychain cloud credential bridge validated | OpenAI and Gemini generic-password entries were found by service name. The localhost-only bridge returned credential-present responses for both providers without printing secrets; provider selection loads credentials into runtime memory only. The one-command launcher starts/reuses the bridge. Live OpenAI Sol generation confirmed the credential path works. |
| 2026-08-16 | OpenAI Sol Aqua Fighter manual run passed MVP Feature Specs | Saved HTML, Markdown, TXT, and JSON packages under `Output Files/OpenAI/`. Inspection found 33 GDD sections, 10 pitch slides, 6/6 valid MVP Feature Specs, six `tddContent` architect records, 13 asset metadata records, and 11 scope critiques with no generation diagnostic. This is the best current qualitative output and should be preserved as a model-agnostic contract/reference fixture, not an exact-text snapshot. |
| 2026-08-16 | Aqua Fighter exposed Final TDD persistence/UI blocker | The UI did not show Final TDD after generation. Export inspection confirms `tddContent.length === 6` but `technicalDesignDocument` is empty; briefs are also absent. Assets and scope remain present. Tomorrow starts by tracing Final TDD request → validation → state commit → generated flag → viewer/package assembly. |
| 2026-08-16 | Global 600-second E2E completion reconfirmed at handoff | The global run completed without browser errors, model-validation errors, transport errors, or sleep interruption. This remains the accepted full-pipeline baseline; Aqua's missing Final TDD is tracked separately as a manual state/persistence/UI defect. |

## Known blockers and cautions

1. Do not use the original outputs as exact-text snapshots; use structural and semantic assertions only.
2. Do not start broad memory/persona changes before BDD and specialist contracts are validated.
3. The current unified pipeline does not preserve critique Q&A; address it only in Milestone 5 after specialist contracts are stable.
4. Current `generateImage` is a placeholder; image status must be handled explicitly in the future visual-pipeline milestone.
5. Existing `start_app.sh` is modified outside this work and must not be overwritten.
6. Uploaded example outputs are currently untracked and must not be altered during implementation unless explicitly requested.
7. Milestone-2 technical contracts are intentionally additive and narrower than the richer canonical design sketch: `TechnicalSpecification` currently centers on feature traceability, BDD scenarios, data models, API/event contracts, state transitions, dependencies, and acceptance criteria. Inputs/outputs, algorithms, validation rules, performance budgets, and detailed failure-handling fields remain deferred.
8. Final TDD document generation instructs the model to preserve feature/scenario IDs, but returned Markdown/JSON sections are not yet semantically checked for complete ID coverage; this belongs in later document-output hardening.

## Resume Here

**Priority next: Reproduce and diagnose the Aqua Fighter Final TDD generation/commit/UI failure without rerunning or altering the saved package.**

Milestones 1–7 and the real-provider E2E baseline are accepted. The sequential compile/MVP inference blocker is resolved through structured LM Studio output, bounded repair, progress-aware compile/MVP waiting, and successful full-package E2E verification.

After persona-recall fixes are complete, begin the queued manual UI/data-capture workstream:

1. Verify every generated stage is visible and remains enabled in the intended manual workflow: MVP, MVP Feature Specs, Final TDD, Freelance Briefs, assets, and scope review.
2. Verify stage outputs persist in the active project state before export, including `mvpFeatureSpecs`, `tddContent`, `technicalDesignDocument`, `productionBriefs`, asset metadata, visual prompt contracts, critique records, memory/persona records, and generation metadata.
3. Verify manual Gemini 3.7 runs use the same complete package assembly path as LM Studio rather than stopping after the early documents.
4. Verify HTML, Markdown, TXT, and JSON exports all contain the same complete structured package and that rich/legacy viewers expose the same records.
5. Convert the manual findings into deterministic UI/persistence/export tests before another real-provider regression run.

```text
current priority: Aqua Final TDD persistence and visibility diagnosis
primary evidence: OpenAI/Aqua Fighter (best quality, valid MVP specs + tddContent, no technicalDesignDocument)
comparison evidence: Gemini/Urban Rally Racing and Mistral/Bluetooth Content Share
accepted today: Keychain credentials work; OpenAI Sol produced 6/6 validated MVP Feature Specs
```

Do not reopen the resolved 600-second MVP timeout design unless a new run demonstrates an actual inactivity stall with no model responses.

### Immediate persona-recall follow-up

1. Manually verify the new persona panel through the Gemini 3.7 workflow and confirm the records are populated rather than merely rendered when present.
2. Continue the queued manual UI/data-capture workstream: verify every generated stage remains visible and enabled, especially MVP Feature Specs, Final TDD, and Freelance Briefs.
3. Add reload/persistence assertions for a real generated session, then verify Gemini 3.7 export parity across HTML, Markdown, TXT, and JSON.

### Immediate MVP/rich/provider retry TODOs (2026-08-16)

- [x] Persist failed MVP Feature Specs/technical-spec diagnostics into project state and the assembled package.
- [x] Render the diagnostic in Rich Package Preview and HTML/Markdown/TXT/JSON exports without committing malformed feature or technical specifications.
- [x] Add deterministic coverage proving Freelance Briefs, Asset List, and Scope Review remain rendered after an MVP diagnostic section.
- [x] Accept common valid technical-spec response envelopes (`technicalSpecification`, `technical_specification`, `specification`, `result`, `output`, and one-item arrays) before applying the unchanged strict technical contract.
- [x] Use `max_completion_tokens` for `o*` and `gpt-5*` models on the OpenAI-compatible Chat Completions path; retain `max_tokens` for LM Studio, GPT-4o, and generic compatible backends.
- [x] Replace the stale rich-coverage fixture reference to the absent `Mistral/Space_Miner_Project_Package.json` with the available complete Bluetooth Content Share fixture.
- [x] Retry MVP Feature Specs with a cloud provider: OpenAI Sol generated and persisted all six Aqua Fighter feature/technical specs without diagnostic.
- [x] Retry the GPT/OpenAI path with Keychain credentials: credential loading and live Sol generation succeeded without the prior token-parameter 400.
- [ ] Run a fresh complete browser workflow and verify Rich Package Preview visibly contains all already-generated sections after a failed MVP retry, especially Freelance Briefs, Asset List, and Scope Review.
- [ ] Do not unlock Final TDD or downstream generation from malformed technical specifications; only a fully validated retry may commit `mvpFeatureSpecs`/`tddContent` and clear the diagnostic.

### Tomorrow — ordered start point (2026-08-17)

1. Preserve `/Output Files/OpenAI/Aqua_Fighter_Project_Package.{json,html,md,txt}` unchanged as the strongest current quality/current-flow fixture.
2. Reproduce the Final TDD action from the saved Aqua project if reload state permits; capture browser console, generation status, provider response shape, and state transitions.
3. Trace `handleGenerateTddDoc`: confirm whether the Sol response is received, parsed, validated, assigned through `setTechnicalDesignDocument`, and followed by `setTddDocGenerated(true)` only after a non-empty commit.
4. Add a deterministic regression test: six valid `tddContent` inputs must either commit non-empty `technicalDesignDocument` and render it, or retain a persistent actionable diagnostic; no silent green/generated state is allowed.
5. Verify the Rich Package and legacy section viewer update immediately after Final TDD commit and remain correct after project reload/export.
6. Make provider/model/run/stage metadata mandatory in assembled and saved packages. Aqua currently does not identify Sol in exported JSON despite being generated with it.
7. Build a structural cross-model comparison from Aqua/Sol, Urban Rally/Gemini, and Bluetooth/Mistral. Keep global rules for traceability, validation, persistence, diagnostics, metadata, and section completeness; keep model adapters limited to token parameters, structured-output capabilities, envelope normalization, retry budgets, and rate limits.
8. After Final TDD is fixed, generate/verify Freelance Briefs for Aqua and confirm all four exports retain GDD → pitch → MVP → feature specs → Final TDD → briefs → assets → scope.